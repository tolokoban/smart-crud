<?php
$ROLE = "";

function isGranted( $inputs ) {
    // @TODO Implement a security test.
    error_log( '[test.tag.request] This service is  not secure!' );
    error_log( 'Please edit the following file: "test.tag.request.security".' );
    return True;
}
?>
